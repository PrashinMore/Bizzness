export interface Expense {
  id: string;
  category: string;
  amount: number;
  note?: string | null;
  date: string; // ISO string
  addedBy: string;
  createdAt: string;
}

export interface Expense {
  id: string;
  category: string;
  amount: number;
  note?: string | null;
  date: string; // ISO string
  addedBy: string;
  createdAt: string;
}


